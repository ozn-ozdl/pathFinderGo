# Copy to .env and adjust as needed.
# (If you use dotenv tooling; the server itself reads from the process environment.)

# HTTP server address
SERVER_ADDR=:8080

# Path to an OSM extract (.osm or .pbf)
OSM_FILE=/absolute/path/to/map.osm

